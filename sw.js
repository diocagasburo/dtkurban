// File: sw.js

// Questo event listener è il minimo indispensabile per far sì che
// il browser riconosca il sito come PWA installabile.
self.addEventListener('fetch', (event) => {
  // Per ora non fa nulla, ma la sua presenza è fondamentale.
});
